---
name: mkfmt
description: Runs `make fmt lint` and fixes errors
---

Run `make fmt lint` and fix any errors reported by the output. If there are "prettier" tool errors it might indicate `make install` should be run, first, to install the linter. 
